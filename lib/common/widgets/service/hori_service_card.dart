import 'package:flutter/material.dart';

class VertServiceCard extends StatelessWidget {
  const VertServiceCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}